export interface Product {
  slug: string;
  name: string;
  description: string;
  entitlementKey: string;
  stripePriceId: string;
  unitAmount: number;
  currency: string;
  seoTitle: string;
  seoDescription: string;
}
